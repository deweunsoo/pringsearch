appId: com.pringsearch.app
productName: Pringsearch
directories:
  buildResources: build
  output: dist
mac:
  category: public.app-category.productivity
  icon: build/icon.icns
  target:
    - dmg
    - zip
dmg:
  artifactName: ${name}-${version}.${ext}
publish:
  provider: github
  owner: deweunsoo
  repo: pringsearch
